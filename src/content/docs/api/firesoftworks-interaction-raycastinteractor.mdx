---
title: "RaycastInteractor"
description: "Implementation of Interactor that uses 3D Raycasting to detect interactable objects.
Perfect for first-person or third-person 3D games."
sidebar:
  label: "RaycastInteractor"
---

**Namespace:** `FireSoftworks.Interaction`  
**Type:** Class  

Implementation of Interactor that uses 3D Raycasting to detect interactable objects.
Perfect for first-person or third-person 3D games.

```csharp
[AddComponentMenu("FireSoftworks/Interaction/Raycast Interactor")]
[DefaultExecutionOrder(-10)]
public class RaycastInteractor : InteractorBase, IInteractor
```

## Methods

### `DetectInteractable()`

Performs a raycast from the camera center to detect interactable objects.

```csharp
protected virtual void DetectInteractable()
```

### `Update()`

```csharp
protected virtual void Update()
```

## Fields

### `interactableMask`

LayerMask determining which objects can be interacted with.

```csharp
public LayerMask interactableMask
```

### `interactionRange`

```csharp
[Header("Raycast Settings")]
public float interactionRange
```

### `playerCamera`

The camera used to determine the raycast origin and direction.

```csharp
public Camera playerCamera
```

